# shared only for local development. production will replace this with OAuth/device enrollment.
ORBE_REMOTE_GATEWAY_URL=ws://127.0.0.1:8787/agent
ORBE_REMOTE_DEVICE_ID=dev-machine
ORBE_REMOTE_DEVICE_TOKEN=change-me-device
ORBE_REMOTE_API_TOKEN=change-me-api
ORBE_REMOTE_PORT=8787
ORBE_REMOTE_AUDIT_PATH=.orbe-remote/audit.jsonl
ORBE_REMOTE_ALLOWED_HOSTS=*
